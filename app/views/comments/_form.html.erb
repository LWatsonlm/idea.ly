<div class= "comments">
<%= form_for([@post, @post.comments.build]) do |f| %>
<h2>Add Comment:</h2>
  <p>
  <%= f.label :name %><br />
  <%= f.text_field :title %><br />
  </p>
  <p>
  <%= f.label :body %><br />
  <%= f.text_field :body %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>
</div>
