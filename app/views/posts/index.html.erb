<h1>Ideas that need your help</h1>

<% @posts.each do |post| %>
  <h2><%= link_to post.title, post_path(post) %></h2>
  <p><%= post.body %></p>
<% end %>

<%= link_to 'add your own idea', new_post_path %>
